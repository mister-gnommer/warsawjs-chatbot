# 🤖 AI-generated
def embed(records: list[dict], override_duplicates: bool) -> int:
    print("[embed] stage not yet implemented — skipping DB insert")
    print(f"[embed] received {len(records)} records, override_duplicates={override_duplicates}")
    return 0
